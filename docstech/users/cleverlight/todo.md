# Todo [](?type=board&ng_level=1&ng_child_type=story&ng_child_status=backlog&ng_view=kanban)


### continue dev setup

+ [ ] chase down log entries
  + can see all logs issued from `client/extension`
  + where are the logs from `client/display`
+ [ ] mark index.tsx HTML as only being used for testing
  + the app's real HTML is generated dynamicalled in `notethinkEditor.ts`


### create language server

+ communicate to separate thread via LSP
  + add to webpack watch


### parse markdown data using NPM module

+ include an NPM module
  + parse documents with [mdast]()


