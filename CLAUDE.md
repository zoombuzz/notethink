# Project instructions

@AGENTS.md

The workspace file below is not a duplicate of the above. It holds the rules the local file defers
to and does not restate: commit policy, story tracking and lifecycle, the shipping gates, dev server
lifecycle, and "finish the whole change". They are binding here. Imported explicitly rather than
left to directory traversal, so they load whichever directory a session starts in.

@../AGENTS.md
